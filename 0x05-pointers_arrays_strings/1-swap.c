#include "main.h"

/**
 * swap_int - swap the value of two integers
 * using two input parameter
 *
 * @a: input parameter
 * @b: input parameter
 *
 * Return: Nothing
*/

void swap_int(int *a, int *b)
{
	int temp;

	*a = temp;
	*a = *b;
	*b = temp;
}
